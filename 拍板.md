这份评审很扎实，尤其补上的 正文状态回收、事实与认知分层、事件溯源，不是优化项，而是会决定 IF 能否长期运行的基础结构。你对整体方向的概括也准确：LLM 扩展候选空间，Jev 压缩候选空间，确定性引擎提交结果。

我查到了 Jev 当前官方接口，下面先回答它的实际能力，再对五个产品决策给出我的倾向。

一、Jev 的实际接口
1. 输入是什么？

Jev 接收：

state：必填，可以是字符串、JSON 对象或数组
questions：一组带 ID 的结构化问题
model：例如 jev-latest

虽然 state 可以使用结构化 JSON，但 Jev 当前理解的输入仍是自然语言文本语义。官方明确说明它目前仅支持文本，不直接支持图片、音频和视频。结构化对象或数组中的内容也应是文本值。

这正适合 IF：

{
  "state": {
    "world": "蒸汽时代王国，魔法受到禁止",
    "character": {
      "name": "林夏",
      "goals": ["阻止顾言离开"],
      "known_facts": ["顾言今晚将乘火车离开"],
      "unknown_facts": ["顾言准备留下"]
    },
    "candidate_event": "林夏决定独自前往车站"
  },
  "questions": {
    "consistent_with_character": {
      "type": "noul",
      "instructions": "这个候选行动是否符合林夏当前的目标、认知与性格？"
    }
  }
}

2. 能否自定义标签和标准？

可以。

Jev 提供三种原语：

Noul：判断一个命题为真的概率，返回 0–1
Choice：从自定义选项集合中选择，返回选项、完整概率分布和置信度
Score：按自定义有序量表评分，返回连续分数、各等级概率和置信度

每个问题都有用户定义的 ID、instructions；Choice 和 Score 还使用 criteria 定义候选项或评分等级。Noul 也可选填 true/false 的明确含义。

这意味着内部 Judge 接口可以如你建议的那样统一：

TruthJudge       → Noul
ChoiceJudge      → Choice
IntensityJudge   → Score
CompletionJudge  → Noul
ComplianceJudge  → 多个 Noul
EquivalenceJudge → Noul
RelevanceJudge   → Score 或 Noul
DistanceJudge    → Score


不过 Score 最多接受 10 个有序等级，它适合强度和距离，不适合大量无序候选。

3. 输出是概率还是标签？

两者都有，但取决于问题类型：

Choice：选中的标签、完整概率分布、置信度
Score：连续分数、等级说明、完整概率分布、置信度
Noul：命题成立的概率

官方将概率描述为经过校准的决策输出，但同时明确指出：校准是针对一组预测衡量的，并不保证单个判断正确。

因此你提出的修正完全正确：

Jev 只提供分布，引擎决定阈值、抽样、保留为趋势，还是交给其他模型复核。

文档中的数据结构不应出现：

{
  "decision": false,
  "confidence": 0.71
}


而应保留原始输出和执行策略：

{
  "candidate_id": "candidate_004",
  "judgment": {
    "type": "noul",
    "probability": 0.71,
    "model": "jev-1.13.0"
  },
  "resolution": {
    "policy": "seeded_sample",
    "random_value": 0.63,
    "accepted": true
  }
}


这样决策可审计，也能重放。

4. 一次能判断多少候选？

官方接口允许在一次请求中混合多个 Choice、Score 和 Noul 问题。所有问题针对同一个 state 并行、相互独立地评估，返回值使用各自的问题 ID 对齐。官方称增加问题对响应时间影响很小。

当前文档给出的上下文上限是：

每请求总预算 64k Token
state + 最长单个问题 上限 32k Token

官方模型页没有在检索内容中给出一个固定的“每次最多多少题”，因此不能据此声称具体数量。实际设计应以 Token 预算和 API 验证为准。

对于 IF，可以把同一份状态下的判断尽量打包：

{
  "questions": {
    "action_consistency": { "...": "..." },
    "knowledge_violation": { "...": "..." },
    "thread_relevance": { "...": "..." },
    "tension_gain": { "...": "..." },
    "scene_complete": { "...": "..." },
    "next_operation": { "...": "..." }
  }
}


但应遵循官方建议，每个问题只判断一件具体、聚焦的事情；多因素决策应拆成多个问题，再由代码组合。

5. 延迟和价格

当前官方模型页列出的 Jev 1.13：

输入价格：每百万 Token 0.042 美元
输出免费
上下文：64k，每份 state 与最长问题组合为 32k
当前速率限制：每秒 250,000 Token、每分钟 1,200 请求
文档提醒这些速率限制正在动态调整，可能变化

官方主页展示过特定工作流下 0.114 秒的演示结果，但这是厂商示例，不应视为 IF 真实场景的稳定延迟保证。

因此工程上应自行记录：

model_version
state_tokens
question_count
latency_ms
probability_distribution
resolution_policy


尤其应该固定版本，例如 jev-1.13.0，而不是长期使用会自动漂移的 jev-latest。官方明确说明别名会随新版本移动，输出可能变化；如果阈值针对特定版本调过，应固定版本 ID。

二、你指出的三个架构缺口，我全部赞同
1. 正文后必须有“状态回收”

正确流程不应只是：

先改状态 → 再写正文


而应形成闭环：

预演状态
→ 生成场景
→ 从正文提取事实
→ 合规检查
→ 提交最终状态


更严格一点，建议区分三种状态：

Proposed State

正文生成前，依据事件预测的变化，用于指导写作，但未成为正式事实。

Observed Facts

正文实际表现出来的新事实，包括：

行动
台词
承诺
伤势
道具
新人物
信息传播
关系变化的外在证据
Committed State

经过提取、判断和校验后正式写入事件日志的结果。

这可以处理“计划写 A，正文意外写成 B”的问题。正式状态必须服从最终呈现给玩家的正文，而不能假装正文没有写过。

你对台词的区分也非常关键：

“王已经死了”


首先产生的是：

{
  "type": "claim",
  "speaker": "char_01",
  "content": "王已经死了",
  "audience": ["char_02"],
  "sincerity": "unknown"
}


而不是：

{
  "fact": "王已经死了"
}


至于边流式生成边检查，我会把它列为后续能力，不作为第一版基础。因为逐段 Jev 检查能发现“已经越界”，却未必能保证在自然句段上干净截断。第一版更稳妥的是：

生成短场景草稿
→ 批量检查
→ 局部修订或重写
→ 再展示


流式体验可以用导演裁定动画填充等待，而不是冒险展示尚未验证的正文。

2. 事实与认知必须是两个系统

甚至建议分成三层：

Reality
世界客观事实

Belief
主体相信什么

Claim
主体向别人表达了什么


因为角色可以：

知道 X，却声称非 X
相信 X，但 X 实际为假
不确定 X，却表现得很确信
听说 X，但不信
曾经相信 X，后来改变观点

认知记录可以设计为：

{
  "subject_id": "char_gu",
  "proposition_id": "prop_lin_betrayed_gu",
  "belief": 0.82,
  "certainty": 0.67,
  "source_event_ids": [
    "event_letter_found",
    "claim_from_char_three"
  ],
  "last_updated_at": "world_time_104",
  "visibility": "private"
}


这确实可能成为 IF 相对酒馆最硬的差异点：

上下文不是简单检索“与角色有关的信息”，而是检索“这个角色在此刻有资格知道的信息”。

人物视角正文只能接收：

客观可感知的当前环境
该角色自己的状态
该角色的 Belief
该角色亲历或被告知的历史
公开知识

不能接收上帝状态中的秘密。

3. Event Sourcing 几乎是必选项

建议唯一真相是：

Event Log + Canonical IF Injections


当前状态、认知、关系和故事线均为投影：

World Projection
Belief Projection
Relationship Projection
Thread Projection
Narrative Projection


但“回溯型 IF”不能简单插入过去后全量重放正文。需要区分：

状态历史可重算
已经展示的正文是玩家体验过的叙事记录
新世界线正文需要重写或标记为旧世界残影

每个事件最好保留：

{
  "event_id": "evt_0192",
  "world_time": "year_12_day_43_morning",
  "narrative_order": 286,
  "caused_by": ["if_014", "evt_0187"],
  "depends_on": ["fact_031", "belief_082"],
  "candidates": [],
  "judgments": [],
  "random_seed": 77492,
  "committed_patch": [],
  "scene_id": "scene_047"
}


你强调的 world_time 与 narrative_order 分离完全正确。

三、我对五个产品决策的建议
1. 沙盒还是游戏？

核心先做沙盒，引擎预留规则集模式。

基础模式：

Sandbox
无限 IF，无胜负，观察世界回应


后续游戏模式：

Challenge
有限 IF、目标、代价、隐藏信息


−log p 作为反事实代价非常漂亮：

cost(IF) = −log P(IF | current_world)


但这里的 p 只能解释为：

Jev 针对明确定义尺度给出的语义可成立程度或自然程度。

不能直接宣传成世界中的客观真实概率。游戏内可以给它一个更贴切的名字：

世界线偏移
因果张力
现实阻力
反事实距离

这保留数学结构，又避免过度解释模型概率。

2. 阈值还是掷骰？

建议混合：

硬事实与合规检查：阈值

例如：

是否违反锁定事实
是否越过认知边界
是否提前解决受保护故事线
IF 是否被正确解析

这些不应随机。

角色行为与事件发展：带种子的概率抽样

例如：

角色是否注意到异常
某个计划是否成功
潜在矛盾是否爆发
下一幕选中哪条均合理的故事线

这些适合掷骰。

导演调度：加权评分后选择

不建议完全抽样，也不建议简单取最高概率：

score =
  semantic_fit
+ tension_gain
+ thread_pressure
+ overdue_bonus
+ character_balance
- repetition_penalty
- premature_resolution_penalty


其中语义项由 Jev给出，机械项由引擎计算。

3. 用户能看到多少？

建议默认采用：

有限上帝视角：看见已确认事实和已呈现的状态变化，但看不见角色秘密、真实意图和未触发趋势。

纯上帝视角会削弱误会、调查和反转；完全角色视角又会削弱 IF 作为“修改世界事实”的策略感。

“观测”可以成为第二项核心动作：

IF
改变世界的一项事实

OBSERVE
获取某个主体或命题的有限信息


但观测结果也不必等于真相全开，可以返回：

当前可见证据
某角色的信念
某个趋势的模糊压力
某条故事线正在积累的异常
4. 世界模拟范围

建议选择：

全世界保持因果状态，只有相关区域进行高分辨率模拟。

可以分三级：

Foreground
当前镜头。逐行动、逐认知模拟。

Active Background
与当前故事线直接相关。按场景或时间段结算。

Dormant World
暂时无关。只保留宏观状态与潜在趋势，不模拟个体行动。


镜头切换到后台区域时，LLM 根据：

上次状态
经过时间
主体目标
世界事件
活跃趋势

提出一组“期间发生了什么”的候选，再由 Jev 裁定和提交。

这样世界会动，但不会为了假装模拟整个大陆而产生不可控成本。

5. IF 的锁定规则

我的默认建议是：

状态型 IF

“此刻成立，之后可以自然变化。”

IF 林夏爱上顾言


建立当前感情状态，但未来可能改变。

规则型 IF

“持续生效，除非被后续 IF 修改。”

IF 所有人无法说谎

真相型 IF

“作为 Canon 永久成立，但认知可以变化。”

IF 骑士一直都是失踪的王储

事件型 IF

“事件确定发生，结果不锁定。”

IF 王宫发生了一场火灾

回溯型 IF

必须让用户选择或由模板预设：

改写
重释
异常

必须显示解析裁定卡。因为用户的最高权限不是决定后果，而是确保系统没有曲解他要注入的事实。

四、现在可以收敛成一版正式架构原则
1. 最小承诺原则
用户的 IF 只建立使其成立所必需的最小事实，不自动承诺任何次生后果。

2. 事实与认知分离原则
世界中为真、主体相信为真、主体声称为真，是三类独立记录。

3. 候选裁定原则
LLM 生成开放候选，Jev 返回概率分布，引擎依据明确策略完成裁定。

4. 状态闭环原则
正文必须经过事实回收与合规检查，已展示的承重事实必须进入事件日志。

5. 事件溯源原则
事件日志是唯一真相；当前状态、关系、认知与故事线均由日志投影得到。

6. 确定性边界原则
Canon、合规和约束使用确定性规则；行为、发现和事件发展允许带种子的概率裁定。

7. 双重时间原则
世界发生时间与叙述呈现顺序分别记录。

8. 分辨率原则
当前镜头高分辨率模拟，相关后台批量结算，无关世界只维护宏观趋势。

9. 导演分权原则
引擎管理节奏结构与硬约束，Jev 提供语义判断，LLM 提供候选与叙事表达。

10. 可替换裁判原则
所有判定通过统一 Judge 接口执行，Jev、LLM 裁判和测试桩均可作为后端。


这份评审之后，IF 已经不再只是“有趣的 AI 文字玩具构想”。它有了比较明确的研究命题：

能否通过事件溯源、认知隔离和概率化戏剧管理，让生成式长篇叙事同时保持开放性、连续性与可干预性。

最小实现不需要马上覆盖世界线重算和后台全世界模拟，但有三项不能推迟：

最小承诺原则
Reality / Belief / Claim 分层
正文后的状态回收

这三项一旦缺失，故事运行十几轮后就会开始出现“玩家读到的世界”和“引擎认为的世界”分裂。