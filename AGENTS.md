# Agent 协作规则

## 文件规模

- 单个源文件原则上不超过 1000 行。
- 新增内容接近上限时，应按职责拆分模块，而不是继续堆叠。
- 修改已有超长文件时，优先在当前任务范围内拆分；无关重构不作为前置条件。

## 真实验证

- 需要真实账号、真实 API、真实设备、真实浏览器体验或主观叙事质量判断的验证，由用户执行或验收。
- Agent 负责准备可运行的验证入口、测试数据、操作步骤和结果记录模板，尽量不自行消耗真实服务额度。
- Agent 仍应完成本地静态检查、单元测试、模拟测试和可自动化的集成测试。
- 无法在本地可靠验证的事项，标记为待用户验证，不把模拟结果当成真实结论。
