IF 是一个由用户注入反事实、由 Jev 执行叙事决策、由 LLM 完成语义建模与文学表达的生成式叙事系统。

三者并不是平级协作，而是分别占据三个不同层面：

用户拥有现实修改权
Jev 拥有叙事决策权
LLM 拥有语义创造与表达权

程序本身则拥有最终的事实存储权和规则执行权。

1. 用户：反事实事件的发起者
专业定位

External Intervention Source，外部干预源

用户不是传统互动小说中的角色操作者，也不是酒馆式对话中的导演。用户不能直接指定角色如何行动、故事如何发展或场景如何结束。

用户拥有的唯一能力是：

向当前世界注入一个必须成立的新条件。

例如：

IF 角色 A 爱上角色 B
IF 王国从未存在过
IF 所有人从此无法说谎
IF 这场战争其实已经结束了


这类输入不是指令，而是一个世界状态断言。

用户的权限边界

用户可以：

创建新的世界事实
修改主体的状态
修改世界运行规则
否定或重新解释既有认知
引入临时事件
对故事施加局部或全局扰动

用户不能直接决定：

某个角色下一步采取什么行动
其他角色如何得知这个事实
该事实会产生哪些后果
故事接下来聚焦哪条线
某个冲突是否结束
正文采用什么视角和节奏

例如：

IF 林夏爱上顾言


它只强制建立：

林夏对顾言的感情状态 = 爱


它不自动意味着：

林夏已经意识到这份感情
林夏愿意承认
林夏会表达感情
顾言会察觉
顾言会回应
原有关系立即改变

这些都属于世界的后续反应，应由系统推演。

用户在系统中的正式定义
User = A source of authoritative counterfactual events


即：

用户是具有权威性的反事实事件源。

“权威性”表示玩家声明的核心条件必须成立；“反事实”表示它可以违反当前趋势、角色意愿或导演计划；“事件源”表示它将和角色行为、世界事件、延迟后果一起进入统一事件管道。

2. Jev：叙事决策器与状态裁定器
专业定位

Narrative Decision Engine，叙事决策引擎

以及：

Semantic State Transition Arbiter，语义状态转移裁定器

Jev 不负责创造完整世界，也不负责书写故事。它负责在 LLM 提出的有限候选中作出结构化判断，并决定哪些候选应当进入正式世界状态。

其核心职责可以分为两部分。

A. 状态裁定

Jev 判断一个事件会对哪些既有状态产生影响，以及候选状态变化是否成立。

例如玩家输入：

IF 林夏爱上顾言


LLM 提出候选影响：

[
  "林夏意识到自己的感情",
  "林夏开始优先保护顾言",
  "林夏减少与顾言见面",
  "顾言察觉林夏行为异常",
  "第三人对林夏产生怀疑"
]


Jev 分别判断：

是否成立
成立概率
影响强度
状态变化方向
是否足以写入正式状态
是否需要继续观察

Jev 的输出不是正文，而是类似：

{
  "self_awareness": {
    "decision": false,
    "confidence": 0.71
  },
  "protective_priority": {
    "decision": true,
    "confidence": 0.83
  },
  "avoidance_behavior": {
    "decision": true,
    "confidence": 0.64
  },
  "gu_suspicion": {
    "decision": true,
    "confidence": 0.58
  }
}

B. 叙事调度

Jev 同时负责导演层面的选择：

当前最值得推进哪个主体
应该处理哪条故事线
哪个临时事件具有最高紧迫度
当前场景应聚焦哪些角色
应继续当前场景，还是切换视角
应推进时间，还是展示即时反应
当前场景是否已经完成
是否应该开放下一次用户干预
当前篇章是否满足结束条件

这意味着 Jev 不直接创作情节，而是从 LLM 提出的情节候选中选择叙事方向。

LLM 负责提出“接下来可能发生什么”
Jev 负责判断“接下来应该展示什么”

Jev 的权限边界

Jev可以决定：

候选状态变化是否成立
候选行动中哪个更符合当前状态
哪个事件获得叙事焦点
故事状态机如何转移
当前场景是否达到停止条件
哪些候选计划被接受

Jev不负责：

发明完整世界观
自由生成新角色
编写人物对白
创作文学性描述
理解任意自然语言后直接修改数据库
保存最终状态
无候选集合地自由创作剧情

Jev 应始终工作于一个有限、可验证的决策空间。

Jev 在系统中的正式定义
Jev = A probabilistic semantic controller operating over constrained narrative candidates


即：

Jev 是在受约束的叙事候选空间中运行的概率语义控制器。

它是导演，但不是作家；是裁判，但不是数据库；是状态机的语义控制层，但不是整个状态机本身。

3. LLM：世界建模器、候选生成器与叙事渲染器
专业定位

LLM 同时承担三个角色：

Semantic World Modeler

语义世界建模器

Narrative Candidate Generator

叙事候选生成器

Narrative Renderer

叙事渲染器

它是系统中工作量最大的部分，但不持有最终决策权。

A. 语义世界建模

开局阶段，LLM 通过会话引导用户确定：

世界观
故事规模
时空范围
主体类别和数量
叙事题材
文风
故事张力
角色和群体
世界规则
代表性物质、地点和现象
初始矛盾与临时事件

随后将自然语言设定转换为结构化主体：

{
  "subjects": [],
  "states": [],
  "relations": [],
  "events": [],
  "world_rules": [],
  "narrative_threads": []
}


LLM 在这里负责“理解这个世界由什么构成”。

B. 候选生成

开放式故事无法让 Jev 凭空做选择，因此 LLM 必须先根据当前状态生成有限候选：

某个角色可能采取的行动
一个事件可能产生的后果
玩家 IF 可能影响的主体
需要新增或修改的状态
当前故事可能推进的方向
下一个场景的候选计划
合理的停止条件

例如：

{
  "candidate_scenes": [
    {
      "id": "scene_a",
      "focus": "林夏开始回避顾言",
      "affected_states": ["distance", "suspicion"]
    },
    {
      "id": "scene_b",
      "focus": "林夏下意识保护顾言",
      "affected_states": ["trust", "self_awareness"]
    },
    {
      "id": "scene_c",
      "focus": "第三人率先察觉林夏的变化",
      "affected_states": ["jealousy", "social_tension"]
    }
  ]
}


Jev 再从中选择、评分或判断。

因此，LLM 负责扩展可能性空间，Jev 负责收缩可能性空间。

C. 叙事渲染

状态和计划确定后，LLM 将结构化结果写成正文：

场景描写
人物行为
对话
心理活动
环境变化
叙事节奏
视角控制
文风一致性

但正文必须服从已批准的 Scene Plan：

{
  "scene_goal": "表现林夏因感情变化而下意识保护顾言",
  "required_beats": [
    "顾言面临一个小型危险",
    "林夏改变原本的行动",
    "顾言注意到林夏的反常"
  ],
  "stop_condition": "顾言开始怀疑林夏改变行动的真正原因",
  "forbidden_resolutions": [
    "不得表白",
    "不得确认顾言的感情",
    "不得解决两人的关系"
  ]
}


LLM写到停止条件成立，即应结束本轮。

LLM 的权限边界

LLM可以：

创造候选主体和状态
解释自然语言 IF
提出状态变化候选
提出人物行为候选
生成场景计划候选
书写正文
提炼历史摘要
发现潜在矛盾

LLM不能自行：

将候选直接写入正式世界
覆盖 Canon
决定关键状态必然发生
解决主要事件
新增永久世界规则
宣布故事结束
绕过 Jev 改变情节方向
LLM 在系统中的正式定义
LLM = A generative semantic layer that expands structured possibilities and renders committed state transitions into narrative


即：

LLM 是生成式语义层，负责扩展结构化可能性，并将已提交的状态变化渲染为叙事文本。

三者的权力关系

可以用一句非常准确的话描述：

The user asserts.
The LLM proposes.
Jev decides.
The engine commits.
The LLM narrates.


中文：

用户声明事实，LLM 提出可能，Jev 作出裁定，引擎提交状态，LLM 完成叙述。

完整流程如下：

用户输入 IF
    ↓
LLM 解析为正式事件
    ↓
引擎锁定玩家指定的核心事实
    ↓
LLM 列举受影响主体和候选状态变化
    ↓
Jev 对候选进行判断、评分和选择
    ↓
引擎提交已批准的状态变更
    ↓
LLM 生成下一场景候选
    ↓
Jev 选择叙事焦点和场景目标
    ↓
LLM 按 Scene Plan 写一个叙事片段
    ↓
Jev 判断场景是否完成
    ↓
引擎保存事实、状态和历史

职责矩阵
能力	用户	Jev	LLM	程序引擎注入新事实	主责	判断影响	解析	保存
创建世界观	引导	不负责	主责	结构校验
创建候选状态	不负责	不负责	主责	Schema 校验
判断状态变化	不负责	主责	提供候选	执行提交
决定叙事焦点	间接影响	主责	提供候选	执行
生成场景规划	不负责	选择与批准	主责	保存
编写正文	不负责	不负责	主责	展示
判断场景结束	不负责	主责	提供完成信号	强制停止
保存正式事实	不负责	不负责	不负责	主责
修改既有历史	通过 IF 发起	判断兼容方式	提出解释	执行策略
决定故事结局	间接影响	主责	生成候选结局	验证与保存
最容易出现的定位错误
错误一：把 Jev 当成另一个 LLM

如果要求 Jev 自由构思“下一步发生什么”，它的优势就不存在了。Jev 应在 LLM 生成的候选集合上做快速、结构化的语义决策。

错误二：让 LLM 直接维护状态

LLM可以提出状态，但不能拥有最终状态。否则同义字段、事实漂移、记忆遗漏和擅自改写会不断累积。

错误三：把用户当导演

如果玩家可以直接要求“让角色表白”“切换到十年后”“让反派失败”，IF 就会退化成普通的 AI 互动小说。

玩家只能声明：

IF 角色决定表白
IF 十年内所有人不会衰老
IF 反派失去了辨别谎言的能力


至于角色能否完成表白、故事是否需要跳到十年后、反派是否因此失败，仍然由系统推演。

错误四：让 Jev 成为事实数据库

Jev 输出的是概率判断，不是持久状态。最终 Canon、State、Event 和 Timeline 必须由确定性程序保存。

最终专业定义

可以直接用于 IF 的设计文档：

用户是外部反事实事件源，负责向叙事世界注入必须成立的新条件，但不直接控制主体行为或剧情发展。

Jev 是概率化叙事决策引擎与语义状态转移裁定器，负责评估候选影响、选择叙事焦点、控制故事状态机，并决定场景何时完成。

LLM 是生成式语义与叙事层，负责世界建模、事件解析、候选生成、场景规划和文学表达，但无权直接提交正式状态或决定关键剧情结果。

再压缩成产品层面的三句话：

用户改变世界的前提。
Jev决定世界如何回应。
LLM讲述回应如何发生。


这应该就是 IF 最清晰、同时也最有辨识度的三方定位。